#!/bin/sh

# Load Xorg configuration.
[[ -f ~/.Xresources ]] && xrdb -all -load ~/.Xresources

# Expose Xresources as environment variables.
. ~/bin/colors.sh

# Background applications.
# TODO: Move to ~/.xsession for Ubuntu?
~/bin/startup.sh &

# Keyboard layout and bindings.
~/bin/keyboard.sh &
