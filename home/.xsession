#!/bin/sh

################################################################
# Startup script for login to custom session type.
################################################################

# Load Xorg configuration.
[ -f ~/.xinitrc ] && . ~/.xinitrc
